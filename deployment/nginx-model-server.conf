# Nginx configuration for Model Server
# Place this file in /etc/nginx/sites-available/ and create a symlink in /etc/nginx/sites-enabled/

# Upstream configuration for the Flask application
upstream stats_server {
    server 127.0.0.1:8085 fail_timeout=0;
}

# HTTP server (redirect to HTTPS if SSL is configured)
server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com;  # Replace with your actual domain or server IP

    # Uncomment these lines if you want to redirect HTTP to HTTPS
    # return 301 https://$server_name$request_uri;

    # Or comment out the SSL server block below and keep HTTP only
    access_log /var/log/nginx/stats.server-access.log;
    error_log /var/log/nginx/stats.server-error.log;

    # Maximum upload size (adjust if needed for large files)
    client_max_body_size 50M;

    # Proxy settings
    location / {
        proxy_pass http://stats_server;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for long-running calculations
        proxy_connect_timeout 900s;
        proxy_send_timeout 900s;
        proxy_read_timeout 900s;

        # Disable buffering for real-time responses
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://stats_server/;
        access_log off;
    }

    # Static files (if any)
    location /static {
        alias /opt/stats.server/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS server (optional - requires SSL certificate)
# Uncomment and configure if you want SSL/TLS
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name your-domain.com;
#
#     # SSL certificate configuration
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/your-domain.com/chain.pem;
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     access_log /var/log/nginx/stats.server-ssl-access.log;
#     error_log /var/log/nginx/stats.server-ssl-error.log;
#
#     client_max_body_size 50M;
#
#     location / {
#         proxy_pass http://stats_server;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_connect_timeout 900s;
#         proxy_send_timeout 900s;
#         proxy_read_timeout 900s;
#
#         proxy_buffering off;
#         proxy_request_buffering off;
#     }
#
#     location /health {
#         proxy_pass http://stats_server/;
#         access_log off;
#     }
#
#     location /static {
#         alias /opt/stats.server/static;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
